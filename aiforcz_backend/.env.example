# AIForcz Backend Configuration
# For local development with SQLite (no additional setup needed):
DATABASE_URL=sqlite:///./aiforcz.db
HOST=127.0.0.1
PORT=8000

# For Render deployment with PostgreSQL:
# DATABASE_URL will be set automatically by Render via the database service